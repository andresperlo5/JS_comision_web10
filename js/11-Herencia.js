/* Herencia */
/* class Persona {
  constructor(nombre, apellido) {
    this.nombre = nombre;
    this.apellido = apellido;
  }

  saludar() {
    console.log(`Hola soy ${this.nombre} ${this.apellido}`);
  }

  mostrarInfo() {
    console.log(this.nombre);
    console.log(this.apellido);
  }
}

class Rol extends Persona {
  constructor(nombre, apellido, rol) {
    super(nombre, apellido);
    this.rol = rol;
  }

  mostrarRol() {
    console.log(`Mi rol es: ${this.rol}`);
  }
} */

//instacia
/* const persona1 = new Persona("andres", "perlo");
console.log(persona1);

const rol1 = new Rol("jose", "gomez", "administrador");
console.log(rol1);
 */
/* class Rol {
  constructor(nombre, apellido, rol) {
    this.nombre = nombre;
    this.apellido = apellido;
    this.rol = rol;
  }

  saludar() {
    console.log(`Hola soy ${this.nombre} ${this.apellido}`);
  }

  mostrarInfo() {
    console.log(this.nombre);
    console.log(this.apellido);
  }

  mostrarRol() {
    console.log(`Mi rol es: ${this.rol}`);
  }
} */
